import { Check, LockKeyhole, MapPin } from 'lucide-react'
import { ModuleHeader } from '../../_components/module-shell'
import { milestones } from '../../_lib/season'
export default function JornadaPage() { return <><ModuleHeader eyebrow="Mapa da temporada" title="Sua Jornada" description="Explore os marcos no seu ritmo. O mapa permanece leve e acessível em qualquer dispositivo."/><section className="sentinela-card relative overflow-hidden rounded-3xl p-6 sm:p-10"><svg className="absolute inset-0 h-full w-full opacity-25" aria-hidden="true" preserveAspectRatio="none"><path d="M60 380 C 170 280, 100 160, 280 170 S 400 330, 560 230 S 700 50, 900 100" fill="none" stroke="#e6b85c" strokeWidth="3" strokeDasharray="8 10"/></svg><ol aria-label="Marcos da jornada" className="relative grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{milestones.map((item,i)=><li key={item.label} className={`min-h-36 rounded-2xl border p-5 ${item.status==='current'?'border-amber-300 bg-amber-300/10':'border-white/10 bg-[#0c1421]/90'}`}><span className="text-xs text-slate-500">MARCO {String(i+1).padStart(2,'0')}</span><div className="mt-4 flex items-center justify-between"><h2 className="font-medium">{item.label}</h2>{item.status==='complete'?<Check className="text-emerald-300"/>:item.status==='current'?<MapPin className="text-amber-300"/>:<LockKeyhole className="text-slate-600"/>}</div><p className="mt-2 text-xs text-slate-500">{item.status==='complete'?'Concluído':item.status==='current'?'Você está aqui':'Complete o marco anterior'}</p></li>)}</ol></section><p className="mt-4 text-xs text-slate-500">Visualização HTML/SVG otimizada. Uma futura experiência 3D será opcional e manterá este mapa como fallback para mobile, movimento reduzido e ausência de WebGL.</p></>}
