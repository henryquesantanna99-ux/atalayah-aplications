import { ModuleHeader } from '../../_components/module-shell'
import { SentinelaAvatar } from '../../_components/sentinela-avatar'
import { getCurrentSquad } from '../../_lib/season'
export default async function Page() { const { squad, members } = await getCurrentSquad(); return <><ModuleHeader eyebrow="Comunidade" title={squad?.name ?? 'Seu squad'} description="Caminhe junto. Celebre avanços, compartilhe pedidos e sustente quem está ao seu lado."/><div className="grid gap-4 md:grid-cols-2">{members.map((member) => <article key={member.id} className="sentinela-card flex items-center gap-4 rounded-2xl p-5"><SentinelaAvatar className="h-16 w-16" label="Avatar do integrante"/><div><h2 className="font-mono text-xs">{member.membership?.user_id ?? member.membership_id}</h2><p className="text-sm text-slate-500">{member.responsibility?.name ?? member.membership?.role ?? 'participante'}</p></div></article>)}{!members.length && <p className="sentinela-card rounded-2xl p-6 text-slate-400">Você ainda não foi alocado a um squad nesta temporada.</p>}</div></> }
