import { ModuleHeader } from '../../../_components/module-shell'
import { getSentinelaContext } from '../../../_lib/data'
export default async function Page() { const { supabase, season } = await getSentinelaContext(); const { data } = await supabase.from('sentinela_memberships').select('*').eq('season_id', season.id).order('created_at'); return <><ModuleHeader eyebrow="Temporada" title="Participantes e mentores" description="Papéis e estados pertencem exclusivamente a esta temporada."/><div className="sentinela-card overflow-x-auto rounded-2xl p-6"><table className="w-full text-left text-sm"><thead><tr className="text-slate-500"><th className="pb-3">Identificador</th><th>Papel</th><th>Status</th></tr></thead><tbody>{data?.map(item=><tr className="border-t border-white/10" key={item.id}><td className="py-3 font-mono text-xs">{item.user_id}</td><td>{item.role}</td><td>{item.status}</td></tr>)}</tbody></table></div></> }
