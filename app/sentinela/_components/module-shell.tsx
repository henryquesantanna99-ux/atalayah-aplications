import { Inbox } from 'lucide-react'
import { Button } from '@/components/ui/button'
export function ModuleHeader({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <header className="mb-8"><p className="text-xs uppercase tracking-[.2em] text-blue-300">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold sm:text-4xl">{title}</h1><p className="mt-2 max-w-2xl text-slate-400">{description}</p></header> }
export function EmptyState({ title = 'Nada por aqui ainda', description = 'Quando houver novidades, elas aparecerão neste espaço.' }: { title?: string; description?: string }) { return <div className="sentinela-card grid min-h-52 place-items-center rounded-2xl p-6 text-center"><div><Inbox className="mx-auto text-slate-500"/><h2 className="mt-3 font-medium">{title}</h2><p className="mt-1 text-sm text-slate-500">{description}</p><Button variant="outline" className="mt-4 border-white/10 bg-white/5">Atualizar</Button></div></div> }
